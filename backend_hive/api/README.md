# AfyaHive PHP API

Deploy this folder to `C:\xampp\htdocs\afyahive`. Run `database/001_afyahive_hardening.sql` once from phpMyAdmin before using the API.

All responses use `{ "success": boolean, "message": string, "data": object|array|null }`.
